---
title: "Component Library Demo"
slug: "999-testing-components"
publishedDate: 2026-02-01
description: "Demo page showcasing all UI components available for blog posts."
coverImage: "../../../assets/images/blog/csp-shield.webp"
tags: ["Components", "Demo"]
draft: true
---

import TLDRSummary from "@components/ui/TLDRSummary.astro";
import CheckList from "@components/ui/CheckList.astro";
import StepByStep from "@components/ui/StepByStep.astro";
import KeyValue from "@components/ui/KeyValue.astro";
import BrowserSupport from "@components/ui/BrowserSupport.astro";
import VersionBadge from "@components/ui/VersionBadge.astro";
import DirectiveCard from "@components/ui/DirectiveCard.astro";
import APIEndpoint from "@components/ui/APIEndpoint.astro";
import BeforeAfter from "@components/ui/BeforeAfter.astro";
import Timeline from "@components/ui/Timeline.astro";
import DecisionTree from "@components/ui/DecisionTree.astro";
import StateNotice from "@components/ui/StateNotice.astro";
import SecurityRating from "@components/ui/SecurityRating.astro";
import Prerequisite from "@components/ui/Prerequisite.astro";
import MemoryMap from "@components/ui/MemoryMap.astro";
import StructPacking from "@components/ui/StructPacking.astro";
import RegisterMap from "@components/ui/RegisterMap.astro";
import ByteFrame from "@components/ui/ByteFrame.astro";
import PacketDiagram from "@components/ui/PacketDiagram.astro";
import SubnetSplit from "@components/ui/SubnetSplit.astro";
import BitwiseOp from "@components/ui/BitwiseOp.astro";
import NumberBases from "@components/ui/NumberBases.astro";
import FloatLayout from "@components/ui/FloatLayout.astro";
import TimingDiagram from "@components/ui/TimingDiagram.astro";
import EncodingDiagram from "@components/ui/EncodingDiagram.astro";
import DeltaCompare from "@components/ui/DeltaCompare.astro";
import LayerStack from "@components/ui/LayerStack.astro";
import CallStack from "@components/ui/CallStack.astro";
import Matrix from "@components/ui/Matrix.astro";
import Pipeline from "@components/ui/Pipeline.astro";
import ForkJoin from "@components/ui/ForkJoin.astro";
import ThemeImage from "@components/ui/ThemeImage.astro";
import FileDownload from "@components/ui/FileDownload.astro";
import demoImage from "../../../assets/images/blog/csp-shield.webp";

This page demonstrates all the new UI components available for blog posts.

---

## Structured Content Components

### TLDRSummary

Collapsible executive summary for key points.

<TLDRSummary title="TL;DR">
- CSP prevents XSS attacks by whitelisting trusted resources
- Use nonces or hashes instead of `'unsafe-inline'`
- Start with `default-src 'none'` and add what you need
- Deploy with `report-only` first to catch issues
</TLDRSummary>

<TLDRSummary title="Quick Summary" collapsed>
This one starts collapsed. Click to expand and see the content.
</TLDRSummary>

---

### CheckList

Styled checklists with semantic icons.

<CheckList title="Required for Strict CSP">
<li data-check="check">Nonces or hashes for inline scripts</li>
<li data-check="check">`object-src 'none'` to block plugins</li>
<li data-check="check">`base-uri 'none'` to prevent injection</li>
<li data-check="warning">`'strict-dynamic'` for dynamic scripts</li>
<li data-check="optional">`upgrade-insecure-requests` for HTTPS</li>
<li data-check="cross">Never use `'unsafe-inline'` for scripts</li>
</CheckList>

---

### StepByStep

Step-by-step guides with numbered circles.

<StepByStep title="Deploying CSP">
<li>
**Configure Nginx**

Add the CSP header to your Nginx configuration file.
</li>
<li>
**Test with Report-Only**

Use `Content-Security-Policy-Report-Only` first to catch violations without breaking your site.
</li>
<li>
**Analyze Reports**

Review violation reports and fix issues in your code.
</li>
<li>
**Enable Enforcement**

Switch to `Content-Security-Policy` when violations are minimal.
</li>
</StepByStep>

---

### KeyValue

Compact key-value tables for configuration.

<KeyValue 
  title="Environment Variables"
  items={[
    { key: "CSP_NONCE", value: "$request_id", description: "Nginx variable for nonce" },
    { key: "REPORT_URI", value: "/csp-reports", description: "Violation report endpoint" },
    { key: "CSP_MODE", value: "enforce", description: "report-only or enforce" },
  ]}
/>

---

## Technical Reference Components

### BrowserSupport

Browser compatibility tables.

<BrowserSupport 
  title="CSP Level 3 Support"
  browsers={[
    { browser: "chrome", version: "59", support: "full" },
    { browser: "firefox", version: "58", support: "full" },
    { browser: "safari", version: "15.4", support: "partial", note: "No strict-dynamic" },
    { browser: "edge", version: "79", support: "full" },
    { browser: "opera", version: "46", support: "full" },
  ]}
/>

---

### VersionBadge

Inline badges for versions and status.

This feature requires <VersionBadge type="version" value="Nginx 1.19+" /> and supports <VersionBadge type="level" value="3" />.

Available badge types:
- <VersionBadge type="version" value="v2.0.0" /> Version
- <VersionBadge type="level" value="3" /> Level
- <VersionBadge type="new" /> New
- <VersionBadge type="deprecated" /> Deprecated
- <VersionBadge type="experimental" /> Experimental
- <VersionBadge type="stable" /> Stable

---

### DirectiveCard

Cards for documenting directives.

<DirectiveCard
  name="script-src"
  syntax="script-src <source> [<source> ...]"
  description="Controls which scripts can be executed on the page. This is the most critical CSP directive for preventing XSS attacks."
  mdnUrl="https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Content-Security-Policy/script-src"
  defaultValue="Inherits from default-src"
  since="CSP Level 1"
/>

<DirectiveCard
  name="strict-dynamic"
  syntax="'strict-dynamic'"
  description="Allows scripts loaded by trusted scripts to also execute. Makes CSP work with dynamically loaded content."
  mdnUrl="https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Content-Security-Policy/script-src#strict-dynamic"
  since="CSP Level 3"
/>

---

### APIEndpoint

Documentation for REST API endpoints.

<APIEndpoint 
  method="POST" 
  path="/api/csp-reports" 
  description="Receives CSP violation reports from browsers."
  auth={false}
>
#### Request Body

```json
{
  "csp-report": {
    "document-uri": "https://example.com/page",
    "violated-directive": "script-src-elem",
    "blocked-uri": "inline"
  }
}
```

#### Response

Returns `204 No Content` on success.
</APIEndpoint>

<APIEndpoint 
  method="GET" 
  path="/api/reports/:id" 
  description="Retrieve a specific violation report by ID."
  auth={true}
/>

---

## Comparison & Demo Components

### BeforeAfter

Side-by-side comparisons.

<BeforeAfter beforeLabel="Without CSP" afterLabel="With CSP">
<div slot="before">
**Vulnerable to XSS**

Any injected script executes without restrictions. Attackers can steal cookies and session tokens.
</div>
<div slot="after">
**Protected**

Only whitelisted scripts execute. Injected scripts are blocked by the browser.
</div>
</BeforeAfter>

---

### Timeline

Vertical timeline for events and history.

<Timeline events={[
  { date: "2012", title: "CSP Level 1", description: "Initial specification released by W3C.", type: "milestone" },
  { date: "2016", title: "CSP Level 2", description: "Added nonces, hashes, and improved directives.", type: "success" },
  { date: "2018", title: "CSP Level 3", description: "Introduced strict-dynamic and other improvements.", type: "success" },
  { date: "2023", title: "report-uri deprecated", description: "Replaced by report-to in the Reporting API.", type: "warning" },
  { date: "Future", title: "CSP Level 4", description: "Ongoing development with new features.", type: "default" },
]} />

---

### DecisionTree

Interactive decision guides.

<DecisionTree question="Which CSP approach should you use?">
<details>
<summary>My site is completely static (no inline scripts)</summary>

Use `script-src 'self'` - the simplest and most secure option. No nonces or hashes needed.
</details>
<details>
<summary>I have a few inline scripts that rarely change</summary>

Use **hashes** (`'sha256-...'`). Generate the hash of each script and add it to your CSP.
</details>
<details>
<summary>My inline scripts change frequently or are generated dynamically</summary>

Use **nonces** with Nginx `sub_filter`. Generate a unique nonce per request.
</details>
<details>
<summary>I use webpack/bundlers that dynamically load chunks</summary>

Use **nonces + 'strict-dynamic'**. This allows dynamically loaded scripts to inherit trust.
</details>
</DecisionTree>

---

## State & Warning Components

### StateNotice

Multi-purpose state banners with various types.

<StateNotice 
  type="deprecated"
  feature="report-uri"
  alternative="report-to"
  alternativeUrl="https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Content-Security-Policy/report-to"
  removalDate="a future browser version"
/>

<StateNotice 
  type="mandatory"
  feature="HTTPS"
  title="Required"
>
HTTPS is now mandatory for modern security. All resources must be served over TLS 1.2 or higher.
</StateNotice>

<StateNotice 
  type="experimental"
  feature="Trusted Types"
>
This feature is experimental and may change in future browser releases.
</StateNotice>

<StateNotice type="breaking" feature="CSP Level 3" title="Breaking Change in v2.0">
The `plugin-types` directive has been removed. Use `object-src 'none'` instead.
</StateNotice>

<StateNotice type="security" feature="eval" />

<StateNotice type="preview" feature="report-to" />

---

### SecurityRating

Security level indicators like Mozilla Observatory.

<SecurityRating 
  rating="A+"
  title="Excellent Security Configuration"
  description="Your CSP includes strict-dynamic, object-src 'none', base-uri 'none', and frame-ancestors 'none'. All security headers are properly configured."
/>

<SecurityRating 
  rating="B"
  title="Good, but can improve"
  description="Consider adding upgrade-insecure-requests and removing 'unsafe-inline' from style-src if possible."
/>

<SecurityRating 
  rating="D"
  title="Needs Improvement"
  description="Your CSP uses 'unsafe-inline' for scripts which defeats the purpose of CSP against XSS attacks."
/>

<SecurityRating rating="F" title="Critical Security Issues" description="No CSP header detected. Your site is vulnerable to XSS attacks." />

---

### Prerequisite

Prerequisites box for tutorials.

<Prerequisite>
#### Knowledge
- Basic understanding of HTTP headers
- Familiarity with Nginx configuration syntax
- Understanding of what XSS attacks are

#### Tools Required
- Nginx `1.19+` with `ngx_http_sub_module`
- Access to server configuration
- Browser DevTools for testing
</Prerequisite>

---

## Combined Examples

Using multiple components together for realistic documentation:

<Prerequisite title="Before You Start">
- Nginx installed and configured
- HTTPS enabled on your domain
- Basic CSP knowledge (see sections above)
</Prerequisite>

<TLDRSummary>
- Set `$cspNonce` from `$request_id`
- Use `sub_filter` to inject nonce into HTML
- Add nonce to CSP header
- Test with DevTools console
</TLDRSummary>

<StepByStep>
<li>
**Add nonce variable**

Set `$cspNonce` in your Nginx server block.
</li>
<li>
**Configure sub_filter**

Enable placeholder replacement for HTML files.
</li>
<li>
**Update CSP header**

Add `'nonce-$cspNonce'` to your `script-src` directive.
</li>
</StepByStep>

<SecurityRating rating="A+" title="Result" description="Following these steps will give you an A+ rating on Mozilla Observatory." />

---

## Memory & Layout Diagrams

Zero-JS, theme-aware diagrams for systems, embedded and C/C++ content.

### MemoryMap

Stacked bars for memory regions (flash/RAM budgets, segment sizing).

<MemoryMap
  title="Where the string pool lives"
  scale="shared"
  bars={[
    { label: "FLASH", segments: [
      { label: "kPool", bytes: 13374, sizeLabel: "13.4 KB" },
      { label: "kOffsets", bytes: 2690, sizeLabel: "2.6 KB" },
    ] },
    { label: "RAM", segments: [{ label: "runtime", bytes: 1 }] },
  ]}
/>

---

### StructPacking

C/C++ struct layout with computed padding holes and `sizeof`.

<StructPacking
  arch="64-bit"
  members={[
    { type: "uint8_t", name: "tag", size: 1 },
    { type: "void*", name: "next", size: 8 },
    { type: "uint16_t", name: "count", size: 2 },
  ]}
/>

---

### RegisterMap

A hardware register's bit-fields; reserved gaps fill automatically.

<RegisterMap
  title="CTRL"
  width={32}
  fields={[
    { name: "ENABLE", bits: 0 },
    { name: "MODE", bits: "2:1" },
    { name: "PRIORITY", bits: "7:4", note: "0–15" },
  ]}
/>

---

### ByteFrame

A single-row byte layout with fixed and variable-length fields.

<ByteFrame
  title="A packed string entry"
  fields={[
    { label: "len", bytes: 1 },
    { label: "UTF-8 bytes", bytes: 5, variable: true },
    { label: "NUL", bytes: 1 },
  ]}
/>

---

## Networking Diagrams

### PacketDiagram

An RFC-style header: fields wrap across 32-bit rows and split on boundaries.

<PacketDiagram
  title="UDP header"
  bitsPerRow={32}
  fields={[
    { name: "Source Port", bits: 16 },
    { name: "Destination Port", bits: 16 },
    { name: "Length", bits: 16 },
    { name: "Checksum", bits: 16 },
  ]}
/>

---

### SubnetSplit

An IPv4 address split into network and host bits, with the derived facts.

<SubnetSplit
  title="192.168.1.10 /26"
  ip="192.168.1.10"
  prefix={26}
/>

---

## Bits & Numbers

### BitwiseOp

A bitwise operation shown bit-by-bit, with the result highlighted.

<BitwiseOp
  title="Masking the low nibble"
  width={8}
  a={0xb2}
  op="&"
  b={0x0f}
  aLabel="flags"
  bLabel="mask"
/>

---

### NumberBases

One integer in hex, decimal, octal and nibble-grouped binary.

<NumberBases
  title="0xB8"
  value={0xb8}
  bits={8}
/>

---

### FloatLayout

An IEEE 754 decode: sign · exponent · mantissa bar, bit groups and value.

<FloatLayout
  title="0.15625 (single precision)"
  value={0.15625}
  precision="single"
/>

---

## Signals & Encoding

### TimingDiagram

A WaveDrom-style digital timing diagram rendered as zero-JS SVG.

<TimingDiagram
  title="SPI mode 0 — single byte"
  signals={[
    { name: "CS", wave: "10......1" },
    { name: "SCLK", wave: "0p......0" },
    { name: "MOSI", wave: "x=.=.=.=x", data: ["b7", "b6", "b5", "b4"] },
    { name: "MISO", wave: "z=.=.=.=z", data: ["r7", "r6", "r5", "r4"] },
  ]}
/>

---

### EncodingDiagram

Maps source tokens to their encoded bytes.

<EncodingDiagram
  title="UTF-8 encoding"
  rows={[
    { label: "A (U+0041)", bytes: ["41"] },
    { label: "é (U+00E9)", bytes: ["C3", "A9"] },
    { label: "€ (U+20AC)", bytes: ["E2", "82", "AC"] },
  ]}
/>

---

## Comparison, Structure & Flow

### DeltaCompare

Before/after metric bars with the delta and an improvement arrow.

<DeltaCompare
  title="What the i18n change saved"
  unit=" B"
  rows={[
    { label: "Index table", before: 5300, after: 2650 },
    { label: "Firmware", before: 1341067, after: 1338903 },
  ]}
/>

---

### LayerStack

A vertical stack of hardware/software layers with side notes.

<LayerStack
  title="Where a tr() call lands"
  layers={[
    { name: "UI / call site", note: "tr(StringId)" },
    { name: "i18n::tr()", note: "range check + fallback" },
    { name: "gen::string()", note: "kPool.data() + offset" },
    { name: "Flash (XIP)", note: "memory-mapped" },
  ]}
/>

---

### CallStack

Call frames with a growth indicator (outermost first).

<CallStack
  title="A tr() lookup, deepest last"
  frames={[
    { name: "ui::drawScreen()" },
    { name: "tr(StringId::PinEnter)", detail: "bounds + fallback" },
    { name: "gen::string(lang, idx)", detail: "kPool.data() + offset" },
  ]}
/>

---

### Matrix

A labelled 2-D grid with optional highlighted cells.

<Matrix
  title="kOffsets — a slice of the table"
  rowHeader="lang"
  cols={["BRAND", "CommonOk", "CommonCancel"]}
  rows={["EN", "ES", "FR"]}
  cells={[
    ["@9379", "@1164", "@12542"],
    ["@9379", "@1164", "@11279"],
    ["@9379", "@1164", "@11901"],
  ]}
  highlight={[
    [0, 0],
    [1, 0],
    [2, 0],
    [0, 1],
    [1, 1],
    [2, 1],
  ]}
/>

---

### Pipeline

Numbered stages with per-stage notes and the artifact on each arrow.

<Pipeline
  title="From source strings to flashed firmware"
  stages={[
    { name: "strings.json", note: "EN · ES" },
    { name: "gen_i18n.py", note: "pack + tail-merge", via: "raw strings" },
    { name: "clang++ -Os", note: "→ .rodata", via: ".cpp" },
    { name: "firmware.elf", note: "13.4 KB pool", via: ".o" },
  ]}
/>

### ForkJoin

A fork → join data-flow: a linear chain splits into parallel artifacts, then merges into a second chain. Highlighted branches, curved SVG connectors and optional phase tags.

<ForkJoin
  title="Build-time generation, runtime lookup"
  beforeLabel="Build time"
  afterLabel="Runtime"
  ariaLabel="The generator forks into kPool and kOffsets, which the accessor joins to return a string the UI renders."
  before={[
    { name: "strings.csv", note: "id, en, es, fr, de, it" },
    { name: "generator", note: "dedup + tail-merge" },
  ]}
  branches={[
    { name: "kPool", note: "one packed blob" },
    { name: "kOffsets", note: "uint16 table" },
  ]}
  after={[
    { name: "gen::string(lang, id)", note: "kPool.data() + offset" },
    { name: "i18n::tr(StringId)" },
    { name: "UI render" },
  ]}
/>

---

## Media & Files

### ThemeImage

A responsive figure that can swap between light and dark variants.

<ThemeImage
  src={demoImage}
  alt="Example diagram used to demonstrate the ThemeImage component"
  caption="A single image shown in both themes"
/>

---

### FileDownload

A download card for an attachment under `public/`.

<FileDownload
  href="/pdf/CV_RequenaPlensJoseManuel_ENG.pdf"
  filename="CV.pdf"
  description="Example download served from the public folder"
/>

---

That's all the components! Each one is designed to be:
- **Accessible** with proper ARIA attributes
- **Theme-aware** for light/dark modes
- **Responsive** for all screen sizes
- **Zero JavaScript** where possible (CSS-only interactions)
