---
/**
 * Spanish Homelab Page.
 * Generates `/es/homelab/` route with Spanish translations.
 */
import "@styles/components/service-stats.css";
import "@styles/components/homelab-components.css";

import InfrastructureInsights from "@components/homelab/InfrastructureInsights";
import ServiceCard from "@components/homelab/ServiceCard.astro";
import ServiceStats from "@components/homelab/ServiceStats";
import BaseLayout from "@layouts/BaseLayout.astro";
import { getSiteUrl } from "@utils/site";

import { defaultLocale, type Locale } from "@i18n/config";
import { useTranslations } from "@i18n/utils";

const locale = (Astro.currentLocale ?? defaultLocale) as Locale;
const t = useTranslations(locale);

interface Provider {
  "@type": string;
  name: string;
}

interface Service {
  id: string;
  name: string;
  title: string;
  icon: string;
  description: string;
  url: string;
  linkText: string;
  statsType: "mastodon" | "matrix" | "meshtastic-combined";
  provider: Provider;
  userInfo?: string;
}

const defaultProvider: Provider = {
  "@type": "Person",
  name: "José Manuel Requena Plens",
};

const services: Service[] = [
  {
    id: "mastodon",
    name: t("pages.homelab.mastodonName"),
    title: "Mastodon",
    icon: "fa-brands:mastodon",
    description: t("pages.homelab.mastodonDescription"),
    url: "https://mstdn.jmrp.io/",
    linkText: t("pages.homelab.mastodonLink"),
    statsType: "mastodon",
    provider: defaultProvider,
  },
  {
    id: "matrix",
    name: t("pages.homelab.matrixName"),
    title: "Matrix",
    icon: "simple-icons:matrix",
    description: t("pages.homelab.matrixDescription"),
    url: "https://matrix.to/#/@jmrplens:matrix.jmrp.io",
    linkText: t("pages.homelab.matrixLink"),
    statsType: "matrix",
    provider: defaultProvider,
    userInfo: "@jmrplens:matrix.jmrp.io",
  },
  {
    id: "meshtastic",
    name: t("pages.homelab.meshtasticName"),
    title: "Meshtastic",
    icon: "fa-solid:broadcast-tower",
    description: t("pages.homelab.meshtasticDescription"),
    url: "https://mesh.jmrp.io",
    linkText: t("pages.homelab.meshtasticLink"),
    statsType: "meshtastic-combined",
    provider: defaultProvider,
  },
];

const servicesSchema = {
  "@context": "https://schema.org",
  "@type": "CollectionPage",
  "@id": new URL(
    "#collection",
    new URL(Astro.url.pathname, getSiteUrl()),
  ).toString(),
  name: "Homelab Infrastructure",
  hasPart: services.map((service) => ({
    "@type": "Service",
    name: service.name,
    description: service.description,
    url: service.url,
    provider: service.provider,
  })),
};
---

<BaseLayout
  title={t("nav.homelab")}
  description={t("pages.homelab.description")}
  schema={servicesSchema}
>
  <section aria-label="Homelab infrastructure">
    <div class="container">
      <h1 class="page-title">{t("pages.homelab.title")}</h1>
      <p class="intro">
        {t("pages.homelab.intro")}
      </p>

      <div class="services-grid">
        {
          services.map((service) => (
            <ServiceCard
              title={service.title}
              icon={service.icon}
              description={service.description}
              link={service.url}
              linkText={service.linkText}
            >
              {service.userInfo && (
                <div
                  slot="extra"
                  class="user-info"
                >
                  {t("pages.homelab.userLabel")} <code>{service.userInfo}</code>
                </div>
              )}
              <div
                id={`${service.id}-stats`}
                class="stats-container"
              >
                <ServiceStats
                  client:visible
                  type={service.statsType}
                />
              </div>
            </ServiceCard>
          ))
        }
      </div>

      <section
        class="infrastructure-insights"
        aria-labelledby="node-nginx-edge-security-analytics"
      >
        <h2
          class="section-subtitle"
          id="node-nginx-edge-security-analytics"
        >
          {t("pages.homelab.nginxNode")}
        </h2>
        <InfrastructureInsights client:visible />
      </section>
    </div>
  </section>
</BaseLayout>

<style>
  .intro {
    max-width: 800px;
    margin-bottom: var(--space-xl);
    font-size: 1.1rem;
    color: var(--color-text-muted);
  }

  .services-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(min(100%, 280px), 1fr));
    gap: var(--space-lg);
  }

  .section-subtitle {
    margin: var(--space-xl) 0 var(--space-lg);
    font-size: 1.5rem;
    font-weight: var(--fw-bold);
    color: var(--color-text-heading);
  }

  .user-info {
    padding: var(--space-sm);
    margin-bottom: var(--space-md);
    font-family: var(--font-mono);
    font-size: 0.9rem;
    color: var(--color-accent);
    overflow-wrap: break-word;
    background: var(--color-bg);
    border-radius: var(--radius-sm);
  }

  code {
    padding: 2px 4px;
    color: var(--color-accent);
    background: var(--color-bg);
    border-radius: 4px;
  }

  .stats-container {
    box-sizing: border-box;
    display: flex;
    flex-direction: column;
    gap: 4px;
    width: 100%;
    padding: 0;
    margin-top: 0;
    font-size: 0.85rem;
    overflow-wrap: break-word;
    background: transparent;
    border: none;
    border-radius: var(--radius-sm);
  }
</style>
