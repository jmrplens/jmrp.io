---
/**
 * Homelab Page.
 * Displays self-hosted services status and details.
 * Uses <ServiceStats> (React) to fetch and display live status.
 */
import "@styles/components/service-stats.css";
import "@styles/components/homelab-components.css";

import InfrastructureInsights from "@components/homelab/InfrastructureInsights";
import ServiceCard from "@components/homelab/ServiceCard.astro";
import ServiceStats from "@components/homelab/ServiceStats";
import BaseLayout from "@layouts/BaseLayout.astro";
import { getSiteUrl } from "@utils/site";

interface Provider {
  "@type": string;
  name: string;
}

interface Service {
  id: string;
  name: string;
  title: string;
  icon: string;
  description: string;
  url: string;
  linkText: string;
  statsType: "mastodon" | "matrix" | "meshtastic-combined";
  provider: Provider;
  userInfo?: string;
}

const defaultProvider: Provider = {
  "@type": "Person",
  name: "José Manuel Requena Plens",
};

const services: Service[] = [
  {
    id: "mastodon",
    name: "Mastodon Instance",
    title: "Mastodon",
    icon: "fa-brands:mastodon",
    description:
      "A decentralized social network server. Part of the Fediverse.",
    url: "https://mstdn.jmrp.io/",
    linkText: "Visit mstdn.jmrp.io",
    statsType: "mastodon",
    provider: defaultProvider,
  },
  {
    id: "matrix",
    name: "Matrix Homeserver",
    title: "Matrix",
    icon: "simple-icons:matrix",
    description:
      "Secure, decentralized communication. My Server: matrix.jmrp.io",
    url: "https://matrix.to/#/@jmrplens:matrix.jmrp.io",
    linkText: "Chat on Matrix",
    statsType: "matrix",
    provider: defaultProvider,
    userInfo: "@jmrplens:matrix.jmrp.io",
  },
  {
    id: "meshtastic",
    name: "Meshtastic Infrastructure",
    title: "Meshtastic",
    icon: "fa-solid:broadcast-tower",
    description:
      "Infrastructure for the Meshtastic mesh network. Includes Map, Node Database, and Network Monitors.",
    url: "https://mesh.jmrp.io",
    linkText: "Open Mesh Hub",
    statsType: "meshtastic-combined",
    provider: defaultProvider,
  },
];

const servicesSchema = {
  "@context": "https://schema.org",
  "@type": "CollectionPage",
  "@id": new URL(
    "#collection",
    new URL(Astro.url.pathname, getSiteUrl()),
  ).toString(),
  name: "Homelab Infrastructure",
  hasPart: services.map((service) => ({
    "@type": "Service",
    name: service.name,
    description: service.description,
    url: service.url,
    provider: service.provider,
  })),
};
---

<BaseLayout
  title="Homelab"
  description="My Homelab Infrastructure & Status"
  schema={servicesSchema}
>
  <section aria-label="Homelab infrastructure">
    <div class="container">
      <h1 class="page-title">Homelab Status</h1>
      <p class="intro">
        I maintain a homelab to support decentralized networks, personal
        infrastructure, and continuous learning. Below is the real-time status
        of my public services.
      </p>

      <div class="services-grid">
        {
          services.map((service) => (
            <ServiceCard
              title={service.title}
              icon={service.icon}
              description={service.description}
              link={service.url}
              linkText={service.linkText}
            >
              {service.userInfo && (
                <div
                  slot="extra"
                  class="user-info"
                >
                  User: <code>{service.userInfo}</code>
                </div>
              )}
              <div
                id={`${service.id}-stats`}
                class="stats-container"
              >
                <ServiceStats
                  client:visible
                  type={service.statsType}
                />
              </div>
            </ServiceCard>
          ))
        }
      </div>

      <section
        class="infrastructure-insights"
        aria-labelledby="node-nginx-edge-security-analytics"
      >
        <h2
          class="section-subtitle"
          id="node-nginx-edge-security-analytics"
        >
          Node: NGINX Edge Security & Analytics
        </h2>
        <InfrastructureInsights client:visible />
      </section>
    </div>
  </section>
</BaseLayout>

<style>
  .intro {
    max-width: 800px;
    margin-bottom: var(--space-xl);
    font-size: 1.1rem;
    color: var(--color-text-muted);
  }

  .services-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(min(100%, 280px), 1fr));
    gap: var(--space-lg);
  }

  .section-subtitle {
    margin: var(--space-xl) 0 var(--space-lg);
    font-size: 1.5rem;
    font-weight: var(--fw-bold);
    color: var(--color-text-heading);
  }

  /* Content Styling for Slots */
  .user-info {
    padding: var(--space-sm);
    margin-bottom: var(--space-md);
    font-family: var(--font-mono);
    font-size: 0.9rem;
    color: var(--color-accent);
    overflow-wrap: break-word;
    background: var(--color-bg);
    border-radius: var(--radius-sm);
  }

  code {
    padding: 2px 4px;
    color: var(--color-accent);
    background: var(--color-bg);
    border-radius: 4px;
  }

  .stats-container {
    box-sizing: border-box;
    display: flex;
    flex-direction: column;
    gap: 4px;
    width: 100%;
    padding: 0;
    margin-top: 0;
    font-size: 0.85rem;
    overflow-wrap: break-word;
    background: transparent;
    border: none;
    border-radius: var(--radius-sm);
  }
</style>
