---
/**
 * CodeTabItem component.
 * Represents a single tab panel within a <CodeTabs> container.
 */
interface Props {
    label: string; // The text to display in the tab button
}

const { label } = Astro.props;
---

<div class="code-tab-panel" data-label={label}>
    <slot />
</div>
